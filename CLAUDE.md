# zfs-playground

Experiment write-ups (`*.md` docs describing a procedure) live in `experiments-docs/`, linked from README's "Complex Experiments" section. Must be extremely concise: terse command blocks, minimal prose. Match the style of the existing docs in that dir.
